{
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": true, // any 허용 안함
    "removeComments": true, //컴파일시 주석제거
    "noImplicitThis": true,
    "alwaysStrict": true,
    "noEmit": true, // 최종 결과물 없음. 단순 타입 체크 용.
    "target": "esnext",
    "lib": ["es2015"] // js로 컴파일 할 때 포함될 라이브러리 목록
    // es2015 for Async/Promise.  dom for DOM API
  },
  "exclude": ["node_modules", "babel.config.js", "metro.config.js"],
  "extends": "expo/tsconfig.base"
}
